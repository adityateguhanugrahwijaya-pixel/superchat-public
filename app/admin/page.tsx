'use client'
import { useEffect, useState } from 'react'

type Data = { packages: { id: string; name: string; daily_token_limit: string; allowed_models: string[] }[]; users: { id: string; name: string; email: string; role: string; packageId: string; banned: boolean }[]; usage: { date: string; tokens: string; users: number }[] }
export default function AdminPage() { const [data, setData] = useState<Data | null>(null); const [error, setError] = useState(''); useEffect(() => { fetch('/api/admin').then(async r => { if (!r.ok) throw new Error('forbidden'); return r.json() }).then(setData).catch(() => setError('This area is restricted to administrators.')) }, []); if (error) return <main className="auth-page"><section className="auth-card"><p className="eyebrow">ADMIN</p><h1>Access restricted</h1><p className="auth-subtitle">{error}</p></section></main>; if (!data) return <main className="auth-page"><p>Loading admin workspace…</p></main>; return <main className="admin-page"><header><div><p className="eyebrow">SUPERCHAT CONTROL ROOM</p><h1>Admin workspace</h1></div><a href="/">Back to chat</a></header><section className="admin-grid"><article><h2>Packages</h2>{data.packages.map(p => <div className="admin-row" key={p.id}><strong>{p.name}</strong><span>{Number(p.daily_token_limit).toLocaleString()} tokens/day</span><small>{p.allowed_models?.length || 0} models</small></div>)}</article><article><h2>Usage, last 30 days</h2>{data.usage.map(u => <div className="admin-row" key={u.date}><strong>{u.date}</strong><span>{Number(u.tokens).toLocaleString()} tokens</span><small>{u.users} users</small></div>)}</article><article className="admin-wide"><h2>Users</h2>{data.users.map(u => <div className="admin-row" key={u.id}><strong>{u.name || u.email}</strong><span>{u.email}</span><small>{u.role}{u.banned ? ' · suspended' : ''}</small></div>)}</article></section></main> }
